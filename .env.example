# Supabase Configuration
# Get these values from your Supabase project dashboard:
# 1. Go to https://app.supabase.com/ and select your project
# 2. Navigate to Project Settings > API
# 3. Copy the URL and keys from there

# The URL of your Supabase project (e.g., https://abcdefghijklm.supabase.co)
VITE_SUPABASE_URL=

# The service role key (used for server-side operations)
# IMPORTANT: This key has admin privileges - keep it secure and never expose it to clients
SUPABASE_SERVICE_KEY=

# The anon/public key (used for client-side operations)
VITE_SUPABASE_ANON_KEY=

# JWT Secret for Authentication
# Used for signing and verifying JWT tokens
# IMPORTANT: Change this to a secure random string in production
JWT_SECRET=tuntas-kilat-secure-jwt-secret

# Optional: YCloud API for WhatsApp messaging
# Get these from your YCloud dashboard if you're using WhatsApp integration
YCLOUD_API_KEY=
YCLOUD_PHONE_NUMBER=

# Optional: Gemini API for AI features
# Get this from Google AI Studio: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=

# Node Environment
# Use 'development' for local development, 'production' for production deployment
NODE_ENV=development